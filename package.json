{
  "name": "notion-offical-api",
  "version": "0.1.1",
  "description": "Turn Notion into CMS by using its offical api",
  "license": "MIT",
  "author": "I-Rinka <I_Rinka@outlook.com>",
  "keywords": [
    "cors",
    "cross-domain",
    "http-proxy",
    "proxy"
  ],
  "main": "./index.js",
  "files": [
    "lib/",
    "test/",
    "Procfile",
    "demo.html",
    "server.js"
  ],
  "dependencies": {
    "dotenv": "^16.0.0",
    "http-proxy": "1.11.1",
    "proxy-from-env": "0.0.1"
  },
  "devDependencies": {
    "coveralls": "^2.11.6",
    "eslint": "^2.2.0",
    "istanbul": "^0.4.2",
    "lolex": "^1.5.0",
    "mocha": "^3.4.2",
    "nock": "^8.2.1",
    "supertest": "^2.0.1"
  },
  "scripts": {
    "lint": "eslint .",
    "test": "mocha ./test/test*.js --reporter spec",
    "test-coverage": "istanbul cover ./node_modules/.bin/_mocha -- test/test.js test/test-ratelimit.js --reporter spec"
  },
  "engines": {
    "node": ">=0.10.0"
  }
}
